# Android Collector Fixtures

Parser fixtures for the Android collector live in `tests/fixtures/android` at the repository root.
The package keeps this directory only as a pointer so collector code does not depend on test files.
