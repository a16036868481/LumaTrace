# PC collector fixtures

Runtime fixtures for tests live in `tests/fixtures/pc`. The collector package keeps this folder as a reminder that parser tests must be fixture-driven and must not require a real Windows process list or PresentMon installation.
